import { CTA, PageHero, SectionHeading, SiteShell } from '@/components/site'
import { processSteps } from '@/lib/site-data'
import { pageMetadata } from '@/lib/metadata'
export const metadata=pageMetadata('Our Engineering Approach','A clear, practical process for reducing uncertainty and shipping working software.','/approach')
export default function Approach(){return <SiteShell><main><PageHero title="Move fast without moving recklessly." copy="Our process is designed to reduce uncertainty, keep communication clear, and get working software into your hands quickly."/><section className="section shell"><SectionHeading eyebrow="THE VECTORSHIP METHOD" title="Five stages. One accountable team."/><div className="detail-sections process-detail mt-12">{processSteps.map(([n,title,copy])=><article key={n}><span className="process-number">{n}</span><div><h3>{title}</h3><p>{copy}</p></div></article>)}</div></section><section className="section approach-section"><div className="shell"><SectionHeading eyebrow="OUR COMMUNICATION STANDARD" title="No surprises. No black boxes."/><div className="mt-12 grid gap-4 md:grid-cols-3">{['Clear milestones','Direct communication','Proactive risk reporting','Regular demos','Written decisions','Transparent progress'].map(item=><div className="profile-card" key={item}><p className="text-lg text-foreground">{item}</p></div>)}</div></div></section><CTA /></main></SiteShell>}
